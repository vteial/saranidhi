name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - '.kiro/**'
  pull_request:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - '*.md'
      - '.kiro/**'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  analyze-test-build:
    name: Analyze, Test & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Run code generation
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Analyze code
        run: flutter analyze --fatal-infos

      - name: Run unit & widget tests with coverage
        run: flutter test --coverage

      - name: Check coverage threshold
        run: |
          if [ ! -f coverage/lcov.info ]; then
            echo "No coverage file found!"
            exit 1
          fi

          # Calculate total line coverage percentage
          TOTAL_LINES=$(grep -c "^DA:" coverage/lcov.info || echo "0")
          HIT_LINES=$(grep "^DA:" coverage/lcov.info | grep -v ",0$" | wc -l || echo "0")

          if [ "$TOTAL_LINES" -eq 0 ]; then
            echo "No coverage data found."
            exit 1
          fi

          COVERAGE=$(( HIT_LINES * 100 / TOTAL_LINES ))
          echo "Coverage: $COVERAGE% ($HIT_LINES/$TOTAL_LINES lines hit)"

          # Threshold: raised from 15% to 25% in Sprint 10
          # Domain layer ~95% covered; UI/presentation layer brings average down
          # Will increase incrementally as widget test coverage improves
          THRESHOLD=25
          if [ "$COVERAGE" -lt "$THRESHOLD" ]; then
            echo "Coverage $COVERAGE% is below the ${THRESHOLD}% threshold!"
            exit 1
          else
            echo "Coverage $COVERAGE% meets the ${THRESHOLD}% threshold."
          fi

      - name: Build web
        run: flutter build web

  integration-test:
    name: Integration Tests (Web)
    runs-on: ubuntu-latest
    needs: analyze-test-build

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install Chrome and ChromeDriver
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Run code generation
        run: dart run build_runner build --delete-conflicting-outputs

      - name: Run integration tests (web)
        run: |
          if [ -d "integration_test" ] && [ "$(find integration_test -name '*_test.dart' | wc -l)" -gt 0 ]; then
            chromedriver --port=4444 &
            sleep 2
            flutter drive \
              --driver=test_driver/integration_test.dart \
              --target=integration_test/app_test.dart \
              -d web-server \
              --browser-name=chrome \
              --headless
          else
            echo "No integration tests found yet — skipping."
          fi
