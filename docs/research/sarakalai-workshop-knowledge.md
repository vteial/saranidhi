[← Back to Docs](../../README.md)

# Saranidhi — Sara Kalai Workshop Knowledge Base

> Canonical digitized capture of the owner's **Sara Kalai (சர கலை / the art of the breath-flow)**
> workshop and class knowledge. Source corpus: the owner's handwritten notes plus recorded
> Tamil classes. This document is **bilingual by design** — Tamil source terms are recorded
> first, each with transliteration and an English gloss. It is captured **incrementally, topic
> by topic**, across many sessions, so it is meant to be resumed and appended to rather than
> written in one pass.
>
> Scope is **Sara Kalai only** — the science of **சரம் (Charam / Swara / breath-flow)**.
> **பஞ்ச பட்சி (Panja Pakshi / five-birds) is intentionally out of scope here**: it is a
> dedicated separate class and will get its own document later (e.g. `panja-pakshi-workshop-knowledge.md`).
>
> This is the **source corpus from which backlog items are later derived** — but **capture ≠ backlog**.
> Each captured practice is classified by *how much the app can actually help* (see Legends). Some
> practices become strong app features; others are pure inner practice the app cannot deliver and are
> kept here as knowledge and context only.

---

## How to use this document

- **One topic per session.** Pick a Sara Kalai topic from your notes/recordings, capture it, then stop. Resume later using the Topic Progress Tracker.
- **Per practice, fill the template.** For every distinct practice inside a topic, copy the [Per-Practice Template](#per-practice-template) and fill each field.
- **Tag its app-help bucket.** Classify every practice 🟢 / 🟡 / 🔴 (see Legends). This is the most important judgement call in the whole document.
- **Mark whether it is already implemented.** We have shipped ~20 basic features; flag ✅ against any practice one of them already realizes, so this doc doubles as a coverage map.
- **Add a rough app idea only if it is obvious.** Leave it blank otherwise. Do not force app-ideas during capture — real backlog items are derived **later (Phase 2b)** from this corpus.
- **Keep Tamil terms with transliteration.** Always record the Tamil term first, then transliteration + English gloss, e.g. **சரம் (Charam / breath-flow)**.

---

## Legends

### Capture status

| Symbol | Meaning |
|--------|---------|
| ⬜ | Pending |
| 🔄 | In progress |
| ✅ | Captured |

### App-help classification (the key distinction)

| Symbol | Bucket | Meaning |
|--------|--------|---------|
| 🟢 | **App-assistable** | The device can compute, remind, track, guide, or visualize this for the user → **strong backlog candidate**. |
| 🟡 | **App-augmentable** | The app cannot do it *for* the user, but it can scaffold: instructions, guided timers, checklists, prompts, self-reported progress → **maybe backlog, maybe just guidance content**. |
| 🔴 | **Self-achieved** | A pure inner practice or attainment the app genuinely cannot deliver → **NOT a backlog item**; documented as knowledge / context only. |

### Implemented flag

| Symbol | Meaning |
|--------|---------|
| ✅ | Implemented (already shipped) |
| ⬜ | Not yet |

---

## Topic Progress Tracker

Use this table to see at a glance what is captured and to resume across sessions.

| # | Sara Kalai Topic | Capture Status | # Practices | Notes |
|---|------------------|:--------------:|:-----------:|-------|
| 1 | [Topic name] | ⬜ Pending | — | — |

*Add a new topic row here as you begin each topic. Update its Capture Status (⬜ → 🔄 → ✅) and practice count as you go.*

---

## Coverage summary (implemented vs corpus)

As topics are captured, the ~20 already-implemented starter features get tagged **✅ Implemented**
against the practices they realize, so this document doubles as a **coverage map** — showing which
parts of the Sara Kalai corpus the app already covers and which remain untouched.

**Implemented: TBD / Total captured: TBD**

For context only (do **not** assert specific mappings here — the owner tags actual mappings per practice),
current app feature modules that *may* map to captured practices include: breath journal / alignment,
astro engine timing (yamas, hora, tattva, rahu kaal), streaks, notifications, rules-based wisdom,
somatic interventions, and the prasanam oracle.

---

## Per-Practice Template

Every captured practice follows this template. Copy the fenced block below for each new practice.

```markdown
#### Practice — <தமிழ் term> (<transliteration> / <English gloss>)

- **Purpose / benefit (the why):** <what this practice is for>
- **How it's practiced (brief mechanics):** <short description of how it is done>
- **App-help bucket:** 🟢 / 🟡 / 🔴 — <one-line reason>
- **Implemented?:** ✅ / ⬜
- **Rough app idea (optional):** <leave blank if none is obvious>
- **Source ref:** <class / video / page — provenance>
```

Below is how a topic section would look once filled in. The practice shown is a
**clearly-fictional placeholder** — replace it with real captured content.

> _Example — replace with real captured content_

#### Practice — [Example practice] ([example-transliteration] / [example English gloss])

- **Purpose / benefit (the why):** [Why this practice exists — replace with the real benefit from your notes.]
- **How it's practiced (brief mechanics):** [Brief mechanics — replace with the real method.]
- **App-help bucket:** 🟡 — [Example reason: the app can offer a guided timer and checklist, but the user does the practice themselves.]
- **Implemented?:** ⬜
- **Rough app idea (optional):** [Optional — leave blank if none is obvious.]
- **Source ref:** [Class / video / page reference.]

---

## Captured Topics

*Topic subsections get appended below as they are captured. Each topic follows the
[Per-Practice Template](#per-practice-template) for every practice it contains, and each topic
starts with a `### <n>. <Topic name>` heading.*

### 1. [First topic — pending capture]

> _Example — replace with real captured content_

#### Practice — [Example practice] ([example-transliteration] / [example English gloss])

- **Purpose / benefit (the why):** [Why this practice exists — replace with the real benefit from your notes.]
- **How it's practiced (brief mechanics):** [Brief mechanics — replace with the real method.]
- **App-help bucket:** 🟡 — [Example reason: the app can scaffold the practice but cannot perform it for the user.]
- **Implemented?:** ⬜
- **Rough app idea (optional):** [Optional — leave blank if none is obvious.]
- **Source ref:** [Class / video / page reference.]

---

[← Back to Docs](../../README.md)
